@using Microsoft.AspNetCore.Identity
@using CowsAndBulls.Areas.Identity.Data
@inject SignInManager<User> SignInManager
@inject UserManager<User> UserManager


@if (SignInManager.IsSignedIn(User))
{
    <div class="row">
        <h4 class="text-gradient">Are you ready</h4>
        
            <div class="form-group">
                <input type="text" class="form-control btn input-cab inputnumber" id="input_guess"name="input_guess" value=""placeholder="Enter 4 digit number" required><br />
                <button type="submit" class="btn input-cab btn-primary ">Submit</button>
                <span></span>
            </div>
        
    </div>
}
else
{
    <h4 class="text-danger">You can plan after the login</h4>
}

